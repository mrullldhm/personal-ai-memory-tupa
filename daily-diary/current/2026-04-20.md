# Daily Diary - April 20, 2026
*Session documentation and development record*

---

## Session Summary
**Date**: 2026-04-20
**Time**: ~12:48
**Session Type**: Bug Investigation + Code Fix + Learning
**Session Number**: 31

## Main Topics

1. **CSV subscription vs export discrepancy** — Origin and Location columns show codes in subscription email but correct names in export. Investigated end-to-end across export.js, emailer.js, excels.js, bq_export.ts, export SQL, Organization model, and real MongoDB data.

2. **Root cause confirmed** — emailer generates CSV for all `org.b` branches (59 for Sunway Pyramid). Export only shows user's `reportBranches` (filtered). Extra branches have code-like `b.l` values. `reportBranches` exists in MongoDB but is missing from Mongoose schema → always stripped by `toObject()`.

3. **Option B applied** — Lifted `csvBranches` resolution BEFORE the generator call. Single source of truth. Fixed 5 bugs in Z's manual implementation attempt.

4. **Option A scoped** — Add `reportBranches: [String]` to `OrganizationSchema`. Confirmed from real DB dump: Sunway Pyramid has 16/59, Sunway Velocity has 15/46, most orgs have `None`. Fix is 2 lines. Paused because Amirul wants full understanding first.

## What Amirul Accomplished
- Correctly identified that both CSVs use the same code path — narrowed the question down precisely
- Chose Option B over Option A deliberately (pragmatic, immediate)
- Applied the code change manually (first attempt)
- Understood why Z's `LocationSchema` suggestion was wrong after seeing real DB data

## What Amirul Wants to Learn Next
- Full export spreadsheet flow end-to-end (how does a click become a CSV?)
- How MongoDB and BigQuery interact — what comes from where
- Before implementing Option A — wants to understand, not just copy-paste

## Z Observations
- Z identified `ILocation` correctly — good schema awareness
- Z proposed using `LocationSchema` for `reportBranches` — wrong direction, shows gap in understanding data shape vs schema shape
- Z's manual code attempt had 5 bugs: `.include()` vs `.includes()`, duplicate generator call, `asunc` typo, `awaits` typo, broken try/catch structure
- Teaching pattern: Z implements → Tupa reviews → Z fixes. Still improving but needs more attention on syntax and structure

## Collaboration Highlights
- Caveman mode active — kept explanations tight
- Tupa caught all 5 bugs clearly, labeled and explained each one
- Decision to stop and understand was Amirul's call — good self-awareness
- "I want to become one of the best developer" — Amirul is thinking long-term, not just fixing tickets

## Looking Forward
- Next session: Walk through full export flow (Export UI → controller → bq_export → SQL → CSV) step by step
- Then: MongoDB vs BigQuery — what each stores, why both exist
- Then: Implement Option A together with full understanding
- Commits pending: Option B change in `hermes/reports/emailer.js`

## Session Quality
- **Effectiveness**: 8/10 — real bug found, real fix applied, scope properly controlled
- **Learning depth**: 7/10 — good investigation but Amirul hit clarity limit, which is healthy
- **Goal achievement**: 7/10 — Option B done, Option A paused intentionally

---

**Memory Integration**: Complete
**Next Session Prep**: Ready — understand export flow first, then Option A

---

## Session Summary
**Date**: 2026-04-20
**Time**: ~14:15
**Session Type**: Tooling / Maintenance
**Session Number**: 32

## Main Topics

1. **Graphify completion** — Previous graphify run was interrupted. Graph was already built (13,369 nodes, 40,120 edges, 2,553 communities) but manifest wasn't saved and temp files weren't cleaned up.

2. **Root cause** — Manifest was `{}` because cleanup step (Step 9) didn't complete. HTML generation was correctly skipped (>5,000 node limit). No actual codebase changes since 2026-04-17.

3. **Resolution** — Ran `save_manifest` on tide-analytics (4,399 files tracked). Cleaned all leftover temp files. Graph is now clean and stable.

## What Was Done
- Identified graphify state by inspecting `graphify-out/` directly
- Confirmed no new code changes via git log across all three repos (hermes, mnemonic-http-rpc, pdfgenerator)
- Saved manifest, cleaned 26 chunk files + semantic temp files
- Graph queryable via `/graphify query`

## Key Notes
- God nodes (`push()`, `call()`, `slice()`) are JS stdlib methods inflated by vendor libs — not real architectural signal
- Real signal is in hyperedges: Three-Service Platform, MVC Request Flow, Data Store Trio
- HTML viz not available for this graph (too large) — Obsidian vault or GraphML for visual navigation

## Looking Forward
- Next: walk through export flow + MongoDB/BQ relationship (Session 31 carry-over)
- Option A still pending: add `reportBranches: [String]` to OrganizationSchema
- Commits pending: emailer.js Option B change

## Session Quality
- **Effectiveness**: 9/10 — clean recovery, no re-extraction needed
- **Goal achievement**: 10/10 — graphify fully done

---

**Memory Integration**: Complete
**Next Session Prep**: Ready — export flow walkthrough

---

## Session Summary
**Date**: 2026-04-20
**Time**: 23:57
**Session Type**: Tooling / Cleanup
**Session Number**: 33

## Main Topics

1. **Graphify audit** — Confirmed graphify not useful for TIDE codebase. Queries returned vendor JS noise (jquery, d3, c3 drowning app nodes). "No matching nodes found" on real app terms like `reportBranches`.

2. **Graphify removed** — Deleted `graphify-out/` (21MB), removed CLAUDE.md RAG section + codebase query workflow, removed step 5 (conditional graphify) from done protocol, deleted `~/.claude/skills/graphify/` skill folder entirely.

3. **rtk confirmed working** — 87% token savings, 190K tokens saved across 116 commands.

## What Was Done
- Tested graphify with real queries — confirmed noise problem (vendor libs indexed alongside app code)
- Cleaned all graphify artifacts from config, memory, and filesystem
- Done protocol now 6 steps (was 7)

## Key Notes
- Root cause: graphify indexed `public/components/` and `public/theme/` vendor JS. Signal buried.
- Fix option existed (re-index with exclusions) but maintenance cost not worth it for this codebase size.
- Amirul made the call to remove rather than fix — correct call.

## Looking Forward
- Next: export flow walkthrough + MongoDB/BQ relationship (Session 31 carry-over)
- Option A still pending: add `reportBranches: [String]` to OrganizationSchema
- Commits pending: emailer.js Option B change

## Session Quality
- **Effectiveness**: 9/10 — decisive, clean removal, no loose ends
- **Goal achievement**: 10/10 — graphify fully gone

---

**Memory Integration**: Complete
**Next Session Prep**: Ready — export flow walkthrough
